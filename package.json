{
  "name": "phaser",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^3.6.1",
    "electron": "^2.0.7",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "main": "src/electron-starter.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "phaser-editor": "concurrently \"yarn start\" \"sleep 5; yarn run electron\""
  }
}
